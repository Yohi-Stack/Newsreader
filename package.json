{ "scripts": {
    "start": "node backend/server.js",
    "build": "npm install && npm install --include=dev --prefix newsApp && npm run build --prefix newsApp",
    "dev": "nodemon backend/server.js"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "nodemon": "^3.1.10",
    "path": "^0.12.7"
  },
  "type" : "module"
}
